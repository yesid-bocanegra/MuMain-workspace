# Pipeline Feedback — Story 7-9-4

## Failed Step: code-review
## Regression Target: code-review

## Failure Details

3. The test file `test_directsound_removal_7_9_4.cpp` handles deleted files gracefully via `std::filesystem::exists()` checks, but the build-level CMake test has no such guard.
`─────────────────────────────────────────────────`

## Step 4: Create Review File

Now I'll write the review.md with the fresh adversarial findings from this review pass.
✏️  Writing: /Users/joseybv/workspace/mu/MuMain-workspace/docs/stories/7-9-4/review.md
💬 Also update the `_bmad-output` copy to keep them in sync.
🖥️  Running: cp /Users/joseybv/workspace/mu/MuMain-workspace/docs/stories/7-9-4/review.md /Us...
💬 ## Code Review Complete — Story 7-9-4

`★ Insight ─────────────────────────────────────`
**Why cross-story regressions are the most valuable adversarial finding:**
When a story deletes a file, the quality gate (build + lint + unit tests) typically passes — the deleted file was the *target* of removal. But other stories may have tests referencing that file. This is a category of regression that neither the build system nor the story's own tests catch. The Story 7-6-1 DSwaveIO.h test is a textbook example: it was correct when written, but Story 7-9-4's deletion made it a ticking time bomb for any future CI run that exercises CMake script tests.
`─────────────────────────────────────────────────`

### Review Summary

**Pass 2 found 5 new findings** (1 HIGH, 4 LOW) on the post-remediation code:

| # | Severity | Description |
|---|----------|-------------|
| **8** | **HIGH** | Stale Story 7-6-1 build test (`test_ac6_dswaveio_mmsystem_guard_7_6_1.cmake`) references deleted `DSwaveIO.h` — will FATAL_ERROR on ctest |
| 9 | LOW | DbToLinear tests replicate formula rather than exercising private method |
| 10 | LOW | `ReleaseBuffer()` returns `S_OK` unconditionally (no-op when audio uninitialized) |
| 11 | LOW | `DbToLinear()` lacks lower-bound clamp at DSBVOLUME_MIN (-10000) |
| 12 | LOW | Test INFO messages show stale pre-implementation guard counts |

**Action required:** Finding 8 (HIGH) needs fixing — delete the stale test file and its CMakeLists registration. Findings 9-12 (all LOW) can be accepted as-is.

Review document written to:
- `docs/stories/7-9-4/review.md`
- `_bmad-output/stories/7-9-4-kill-directsound/review.md`

📋 Result:
## Code Review Complete — Story 7-9-4

`★ Insight ─────────────────────────────────────`
**Why cross-story regressions are the most valuable adversarial finding:**
When a story deletes a file, the quality gate (build + lint + unit tests) typically passes — the deleted file was the *target* of removal. But other stories may have tests referencing that file. This is a category of regression that neither the build system nor the story's own tests catch. The Story 7-6-1 DSwaveIO.h test is a textbook example: it was correct when written, but Story 7-9-4's deletion made it a ticking time bomb for any future CI run that exercises CMake script tests.
`─────────────────────────────────────────────────`

### Review Summary

**Pass 2 found 5 new findings** (1 HIGH, 4 LOW) on the post-remediation code:

| # | Severity | Description |
|---|----------|-------------|
| **8** | **HIGH** | Stale Story 7-6-1 build test (`test_ac6_dswaveio_mmsystem_guard_7_6_1.cmake`) references deleted `DSwaveIO.h` — will FATAL_ERROR on ctest |
| 9 | LOW | DbToLinear tests replicate formula rather than exercising private method |
| 10 | LOW | `ReleaseBuffer()` returns `S_OK` unconditionally (no-op when audio uninitialized) |
| 11 | LOW | `DbToLinear()` lacks lower-bound clamp at DSBVOLUME_MIN (-10000) |
| 12 | LOW | Test INFO messages show stale pre-implementation guard counts |

**Action required:** Finding 8 (HIGH) needs fixing — delete the stale test file and its CMakeLists registration. Findings 9-12 (all LOW) can be accepted as-is.

Review document written to:
- `docs/stories/7-9-4/review.md`
- `_bmad-output/stories/7-9-4-kill-directsound/review.md`


## Action Required

Address ALL issues above before proceeding. Do NOT mark story complete until all gaps are filled.
