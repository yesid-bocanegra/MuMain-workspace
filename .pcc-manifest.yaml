# PCC Manifest — Generated by workspace-configure
# Date: 2026-02-24
# DO NOT EDIT MANUALLY — regenerated by PCC workspace-configure workflow

pcc:
  version: "3.0.0"
  generated_at: "2026-02-24T14:45:00-05:00"

config_hash: "685d584439e3ecc57839a458dfdacda55cfb8e58a3f2bb974189fad60a71283e"

# =============================================================================
# GENERATED FILES
# =============================================================================
# Files generated or modified by workspace-configure.
# SHA256 hashes detect manual edits — PCC warns before overwriting.

generated_files:
  ".pcc-config.yaml":
    sha256: "685d584439e3ecc57839a458dfdacda55cfb8e58a3f2bb974189fad60a71283e"
    step: "0"
    description: "Project configuration"

  ".gitignore":
    sha256: "9039e48417ef119d62b6c0f1ebfb2161ee57d29c872d155d848fa0defbfccaff"
    step: "6"
    description: "Workspace gitignore with C++ build artifact extras"

# =============================================================================
# SKIPPED STEPS
# =============================================================================
# Steps auto-skipped based on project type (native desktop C++ client)

skipped_steps:
  - step: "3"
    reason: "No Docker — native desktop game client"
  - step: "4"
    reason: "No Docker dev overlay needed"
  - step: "5"
    reason: "No ctl script — no Docker Compose"
  - step: "7"
    reason: "No e2e framework scripts — not a web app"
  - step: "8"
    reason: "CI lives inside MuMain submodule, not at workspace level"

# =============================================================================
# COMPONENT STATUS
# =============================================================================

components:
  mumain:
    path: "./MuMain"
    type: "git-submodule"
    remote: "git@github.com:yesid-bocanegra/MuMain.git"
    branch: "main"
    commit: "6a285e10af157c1888f93fd040d20752d8a2bda4"

  project-docs:
    path: "./_bmad-output"
    type: "local-directory"
    status: "empty-scaffold"

# =============================================================================
# CONSISTENCY CHECKS (Step 9)
# =============================================================================

consistency_checks:
  service_name_consistency:
    status: "SKIP"
    reason: "No Docker services"
  port_conflict:
    status: "SKIP"
    reason: "No Docker services"
  network_consistency:
    status: "SKIP"
    reason: "No Docker services"
  ci_config_alignment:
    status: "SKIP"
    reason: "CI managed inside MuMain submodule"
  component_health:
    status: "PASS"
    details:
      - "mumain: submodule checked out on main, remote URL matches config"
      - "project-docs: directory exists with expected subdirectories"
  file_existence:
    status: "PASS"
    details:
      - ".pcc-config.yaml exists (103 lines)"
      - ".gitignore exists and updated with workspace extras"

# =============================================================================
# QUALITY TOOLS (Step 9.5)
# =============================================================================

quality_validation:
  overall_status: "COMPLETE"
  components:
    mumain:
      status: "PASS"
      tools:
        - ".clang-format: present"
        - ".clang-tidy: present"
        - ".cppcheck: present"
        - ".editorconfig: present"
    project-docs:
      status: "SKIP"
      reason: "Documentation component — no quality tools needed"

# =============================================================================
# AUTHORED DIRECTORIES (not managed by PCC)
# =============================================================================

authored_directories:
  - "docs/"
  - "MuMain/src/"
  - "_bmad-output/planning-artifacts/"
  - "_bmad-output/implementation-artifacts/"
  - "_bmad-output/test-artifacts/"
