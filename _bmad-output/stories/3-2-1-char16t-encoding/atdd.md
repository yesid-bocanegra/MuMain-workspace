# ATDD Checklist — Story 3.2.1: char16_t Encoding at .NET Interop Boundary

**Story Key:** `3-2-1-char16t-encoding`
**Story Type:** `infrastructure`
**Flow Code:** `VS1-NET-CHAR16T-ENCODING`
**Date:** 2026-03-07
**Phase:** RED (tests created, implementation pending)

---

## PCC Compliance Summary

| Check | Status | Detail |
|-------|--------|--------|
| Guidelines loaded | PASS | `project-context.md` + `development-standards.md` loaded |
| Prohibited libraries | PASS | No mocking framework, no raw new/delete, no NULL |
| Testing framework | PASS | Catch2 v3.7.1 — `TEST_CASE`/`SECTION`/`REQUIRE`/`CHECK` |
| No Win32 in tests | PASS | Tests use only `<string>`, `<cstring>`, `PlatformCompat.h` |
| Story type | `infrastructure` | Unit + CMake-script integration; no Bruno, no E2E |
| Existing tests mapped | PASS | No prior tests found for 3-2-1 ACs — all new |
| AC-N prefixes | PASS | All TEST_CASE names carry `AC-N:` prefix in title |

---

## Test Files Created (RED Phase)

| File | Type | ACs Covered |
|------|------|-------------|
| `MuMain/tests/platform/test_char16t_encoding.cpp` | Catch2 unit | AC-4, AC-5, AC-STD-2, AC-VAL-1, AC-VAL-2 |
| `MuMain/tests/build/test_ac_std11_flow_code_3_2_1.cmake` | CMake script | AC-1, AC-3, AC-STD-3, AC-STD-11, AC-VAL-3 |

---

## AC-to-Test Mapping

| AC | Description | Test Method(s) | File |
|----|-------------|----------------|------|
| AC-1 | `Connection::Connection(const char16_t* host, ...)` + `Connect` typedef | `3.2.1-AC-STD-11:flow-code-traceability` (Check 4+5) | `test_ac_std11_flow_code_3_2_1.cmake` |
| AC-2 | `Marshal.PtrToStringUni` in .NET export | Not covered by automated C++ test — verified in dev-story by code inspection of `ConnectionManager.cs` | — |
| AC-3 | `Common.xslt` String nativetype = `const char16_t*` | `3.2.1-AC-STD-11:flow-code-traceability` (Check 7+8) | `test_ac_std11_flow_code_3_2_1.cmake` |
| AC-4 | `mu_wchar_to_char16` + `mu_char16_to_wchar` utilities | `AC-4: mu_wchar_to_char16 -- Latin ASCII round-trip`, `AC-4: ... Korean BMP round-trip`, `AC-4: ... mixed script round-trip`, `AC-4: ... null input returns empty`, `AC-4: ... IP address ASCII round-trip`, `AC-4: ... empty string round-trip` | `test_char16t_encoding.cpp` |
| AC-5 | Korean/Latin/mixed-script round-trip through C++→.NET boundary | `AC-4: ... Korean BMP round-trip`, `AC-4: ... mixed script round-trip`, `AC-4: ... IP address ASCII round-trip` | `test_char16t_encoding.cpp` |
| AC-STD-2 | Catch2 byte-level encoding tests (Korean baseline + wchar_t size coverage) | `AC-STD-2: mu_wchar_to_char16 -- compile-time wchar_t size is documented`, `AC-STD-2: mu_wchar_to_char16 -- non-BMP codepoint produces surrogate pair`, `AC-VAL-2: Byte-level UTF-16LE baseline comparison` (SECTION inside Korean test) | `test_char16t_encoding.cpp` |
| AC-STD-3 | No `wchar_t` at .NET interop boundary | `3.2.1-AC-STD-11:flow-code-traceability` (Check 5+8) | `test_ac_std11_flow_code_3_2_1.cmake` |
| AC-STD-11 | `VS1-NET-CHAR16T-ENCODING` flow code in `Connection.h` | `3.2.1-AC-STD-11:flow-code-traceability` (Check 2+3) | `test_ac_std11_flow_code_3_2_1.cmake` |
| AC-VAL-1 | Catch2 round-trip tests pass | All `AC-4:` TEST_CASEs | `test_char16t_encoding.cpp` |
| AC-VAL-2 | Korean byte baseline `{5C D5 6D AD B4 C5}` matches | `AC-4: mu_wchar_to_char16 -- Korean BMP round-trip` → SECTION `AC-VAL-2` | `test_char16t_encoding.cpp` |
| AC-VAL-3 | CMake script: flow code + no-wchar_t in Connection.h and Common.xslt | `3.2.1-AC-STD-11:flow-code-traceability` | `test_ac_std11_flow_code_3_2_1.cmake` |

> **AC-2 note:** `Marshal.PtrToStringUni` is a .NET-only change in `ConnectionManager.cs`. It cannot be tested from a C++ Catch2 binary. Verification is by code inspection during dev-story implementation (check the diff of `ConnectionManager.cs`).

---

## Implementation Checklist

All items start as `[ ]` (pending). Implementation fills them in during dev-story.

### AC-1: Connection.h / Connection.cpp — char16_t constructor

- [ ] `Connection::Connection(const char16_t* host, ...)` declaration in `Connection.h`
- [ ] `Connect` typedef in `Connection.cpp` uses `const char16_t*`
- [ ] Constructor definition in `Connection.cpp` uses `const char16_t*`
- [ ] `// Flow Code: VS1-NET-CHAR16T-ENCODING` comment added to `Connection.h`

### AC-2: ConnectionManager.cs — Marshal.PtrToStringUni

- [ ] `Marshal.PtrToStringAuto(hostPtr)` → `Marshal.PtrToStringUni(hostPtr)` in `ConnectionManager_Connect`
- [ ] Verified by diff inspection: no `PtrToStringAuto` remains for `hostPtr`

### AC-3: Common.xslt — String nativetype

- [ ] Line 94 (or equivalent) changed from `const wchar_t*` to `const char16_t*` for `String` nativetype template
- [ ] Generated files (`PacketBindings_*.h`, `PacketFunctions_*.h/.cpp`) NOT hand-edited — regenerated by .NET publish

### AC-4: PlatformCompat.h — Conversion utilities

- [ ] `mu_wchar_to_char16(const wchar_t*)` → `std::u16string` added to `PlatformCompat.h`
- [ ] `mu_char16_to_wchar(const char16_t*)` → `std::wstring` added to `PlatformCompat.h`
- [ ] `if constexpr (sizeof(wchar_t) == sizeof(char16_t))` path: `reinterpret_cast` (Windows/MinGW)
- [ ] `else` path: UTF-32 → UTF-16 transcode with surrogate pair support (Linux/macOS)
- [ ] Null-pointer guard: returns empty string for `nullptr` input (both functions)
- [ ] `#include <string>` present in `PlatformCompat.h` for `std::u16string`/`std::wstring`

### AC-4 (callers): WSclient.cpp and UIWindows.cpp

- [ ] `WSclient.cpp` `CreateSocket`: `mu_wchar_to_char16(IpAddr)` used; `Connection(host16.c_str(), ...)` called
- [ ] `UIWindows.cpp` ~line 1459: Connection construction site verified and updated if `wchar_t*` host param
- [ ] `UIWindows.cpp` ~line 3762: Connection construction site verified and updated if `wchar_t*` host param

### AC-5: Round-trip correctness

- [ ] `mu_wchar_to_char16` / `mu_char16_to_wchar` round-trips Korean, Latin, mixed, and IP strings
- [ ] Byte-level UTF-16LE output for `L"\xD55C\xAD6D\xC5B4"` = `{5C D5 6D AD B4 C5}` on all platforms

### AC-STD-2: Catch2 tests

- [ ] `MuMain/tests/platform/test_char16t_encoding.cpp` created (RED phase file exists)
- [ ] Registered in `MuMain/tests/CMakeLists.txt` via `target_sources(MuTests PRIVATE ...)`
- [ ] All TEST_CASEs compile (even if failing at runtime in RED phase)
- [ ] Tests pass after implementation (GREEN phase)

### AC-STD-3: No wchar_t at interop boundary

- [ ] `Connection.h` contains no `const wchar_t*` in constructor or `Connect` typedef
- [ ] `Common.xslt` String nativetype template does not output `const wchar_t*`
- [ ] CMake script test `test_ac_std11_flow_code_3_2_1.cmake` passes

### AC-STD-4 / AC-STD-13: Quality gate

- [ ] `./ctl check` passes (clang-format + cppcheck) — zero violations
- [ ] `cmake --preset macos-arm64` configures cleanly
- [ ] MinGW cross-compile (`-DMU_ENABLE_DOTNET=OFF`) passes

### AC-STD-11: Flow code traceability

- [ ] `VS1-NET-CHAR16T-ENCODING` in `Connection.h` header comment
- [ ] `VS1-NET-CHAR16T-ENCODING` in commit message
- [ ] CMake script `3.2.1-AC-STD-11:flow-code-traceability` test passes

### AC-VAL-1: Round-trip tests pass

- [ ] All `AC-4:` TEST_CASEs in `test_char16t_encoding.cpp` pass with CTest

### AC-VAL-2: Korean byte baseline

- [ ] `AC-4: mu_wchar_to_char16 -- Korean BMP round-trip` SECTION `AC-VAL-2` passes
- [ ] Byte output `{5C, D5, 6D, AD, B4, C5}` confirmed on CI (MinGW, 2-byte wchar_t)

### AC-VAL-3: CMake script checks

- [ ] `MuMain/tests/build/test_ac_std11_flow_code_3_2_1.cmake` created (RED phase file exists)
- [ ] Registered in `MuMain/tests/build/CMakeLists.txt` as `3.2.1-AC-STD-11:flow-code-traceability`
- [ ] CMake test passes with `cmake -P` after implementation

### AC-STD-NFR-1: Conversion overhead

- [ ] `mu_wchar_to_char16` on ASCII (IP address): no-op path on Windows (`sizeof(wchar_t)==2` → `reinterpret_cast`)
- [ ] No unnecessary heap allocation for ASCII-only strings on the Windows path

### AC-STD-NFR-2: BMP character correctness

- [ ] Korean Hangul syllables (U+AC00–U+D7A3) convert without truncation or corruption
- [ ] Verified by Korean round-trip test and byte-level baseline

### PCC Compliance Items

- [ ] No prohibited libraries used in test files (no mocking framework, no raw new/delete)
- [ ] All test files use `REQUIRE` / `CHECK` and `TEST_CASE` / `SECTION` (Catch2 v3.7.1)
- [ ] No Win32 API calls in test code
- [ ] `#pragma once` in all new/modified headers
- [ ] `nullptr` used (not `NULL`) in new code
- [ ] No `wprintf` in new code — `g_ErrorReport.Write()` for errors
- [ ] Allman braces + 4-space indent in all new C++ files

---

## Files to Verify (post-implementation)

| File | Expected Change |
|------|----------------|
| `MuMain/src/source/Dotnet/Connection.h` | `const char16_t*` constructor + flow code comment |
| `MuMain/src/source/Dotnet/Connection.cpp` | `Connect` typedef + constructor definition with `const char16_t*` |
| `MuMain/src/source/Platform/PlatformCompat.h` | `mu_wchar_to_char16` + `mu_char16_to_wchar` inline utilities |
| `MuMain/src/source/Network/WSclient.cpp` | `mu_wchar_to_char16` call in `CreateSocket` |
| `MuMain/src/source/UI/Legacy/UIWindows.cpp` | `mu_wchar_to_char16` at two Connection construction sites |
| `MuMain/ClientLibrary/Common.xslt` | `const char16_t*` for String nativetype (line 94) |
| `MuMain/ClientLibrary/ConnectionManager.cs` | `Marshal.PtrToStringUni` replacing `PtrToStringAuto` |
| `MuMain/tests/platform/test_char16t_encoding.cpp` | Created (RED phase) |
| `MuMain/tests/build/test_ac_std11_flow_code_3_2_1.cmake` | Created (RED phase) |
| `MuMain/tests/CMakeLists.txt` | `target_sources(MuTests PRIVATE platform/test_char16t_encoding.cpp)` |
| `MuMain/tests/build/CMakeLists.txt` | `3.2.1-AC-STD-11:flow-code-traceability` test registered |

---

## Bruno Quality Checklist

Not applicable — `infrastructure` story type, no REST API endpoints.

---

## Output Summary

- **Story:** 3-2-1-char16t-encoding
- **Primary test level:** Unit (Catch2) + CMake-script integration
- **Failing tests created:**
  - Catch2: 9 TEST_CASEs (7 top-level + 2 SECTIONs) in `test_char16t_encoding.cpp`
  - CMake script: 8 checks in `test_ac_std11_flow_code_3_2_1.cmake` (1 CTest entry)
- **AC-2 exception:** `Marshal.PtrToStringUni` verified by code inspection (no C++ test possible for .NET-only change)
- **Output file:** `_bmad-output/stories/3-2-1-char16t-encoding/atdd.md`
- **All checklist items:** `[ ]` (pending implementation)
